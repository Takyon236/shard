# THE REJECTION BRANCH, and it is the benign input most people forget to declare.
#
# `load` raises on the line below, `witness.py` catches it and exits 0. That is the loader working as
# designed — and without this file, a finding whose only evidence was "the program errored" would look
# like a demonstration.
retries = = =
