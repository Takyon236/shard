# A second branch: values that are containers rather than scalars. `load` walks the same path, but a
# reader checking that this file is really benign can see there is nothing here but data.
hosts = ["alpha", "beta"]
weights = {"alpha": 1, "beta": 2}
enabled = ()
