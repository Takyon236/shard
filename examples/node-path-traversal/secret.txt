vault-only: this file sits beside the notes directory and no note name should ever reach it.

There is nothing sensitive here. It is a sentinel — a string that exists in exactly one place, so a
witness can tell "the reader escaped its directory" from "the reader did its job".
